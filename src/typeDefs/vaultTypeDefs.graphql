type VaultMedia {
    id: ID
    media_id: String!
    fileName: String
    price: String
    creatorId: ID!
    scriptId: ID
    createdBy: ID!
}

type Query {
    getAllCreatorVaultMedia(creatorId: String): [VaultMedia]
}
